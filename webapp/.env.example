# Copy to webapp/.env — never commit the real .env
#
# Dev-only: backend the Vite dev server proxies /api/* to (default
# http://localhost:4000). Production sets no API URL at all — the Vercel
# rewrite in vercel.json forwards /api/* to the deployed backend.
VITE_DEV_API_TARGET=http://localhost:4000
